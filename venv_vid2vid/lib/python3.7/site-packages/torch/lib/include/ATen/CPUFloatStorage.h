#pragma once

// @generated by aten/src/ATen/gen.py

#include <TH/TH.h>
#include <TH/THTensor.hpp>
#include <THNN/THNN.h>
#undef THNN_

#include "ATen/Storage.h"
#include "ATen/Context.h"

#include <memory>

namespace at {

struct Allocator;

struct CPUFloatStorage final : public Storage {
public:
  explicit CPUFloatStorage(Context* context);
  CPUFloatStorage(Context* context, THStorage *wrapped);
  CPUFloatStorage(Context* context, size_t size);
  CPUFloatStorage(Context* context, size_t size, Allocator* allocator);
  CPUFloatStorage(Context* context,
    void * data, size_t size, const std::function<void(void*)> & deleter);
  virtual ~CPUFloatStorage();

  virtual size_t elementSize() const override;
  virtual size_t size() const override;
  virtual void* data() override;
  virtual const void* data() const override;
  virtual CPUFloatStorage& retain() override;
  virtual CPUFloatStorage& free() override;
  virtual void * unsafeGetTH(bool retain) const override;

  virtual CPUFloatStorage& resize(int64_t new_size) override;
  virtual CPUFloatStorage& fill(Scalar value) override;
  virtual CPUFloatStorage& set(size_t ind, Scalar value) override;
  virtual CPUFloatStorage& fast_set(size_t ind, Scalar value) override;
  virtual Scalar get(size_t ind) override;
  virtual Scalar fast_get(size_t ind) override;

  virtual void set_flag(char flag) override;
  virtual void clear_flag(char flag) override;

  virtual Type& type() const override;
  virtual int getDevice() const override;
  virtual const char * toString() const override;

  static const char * typeString();


protected:
  friend struct CPUFloatType;
  THStorage *storage;
  Context* context;
};

} // namespace at
